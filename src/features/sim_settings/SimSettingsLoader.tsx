// src/features/sim_settings/SimSettingsLoader.tsx

import SimSettings from './pages/SimSettings';

export default function SimSettingsLoader() {
  // All data fetching now lives inside SimSettings itself.
  return <SimSettings />;
}
